var router = require("express").Router();
var controllers = require()

router.route("/dashboard")
  .get()
  .post();


router
  .route("/:Profile")
  .get()
  .put()
  .delete();

module.exports = router;
